# Editor / IDE
.vscode/
.idea/

# Python caches
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv
.env
venv/
.venv/

# Local logs / runtime artifacts
logs/
*.log
output/

# results/ 只放行 README.md(研究敘事);其餘皆不入版控 —— 圖(figures/*.png)是
# plot_results.py 的生成產物、threads_post / gemini_prompts 是拋棄式素材、json/raw txt
# 是資料 artifact(以重現指令重跑取得)。只 un-ignore README,不必個別再忽略草稿。
results/**
!results/README.md

# Claude planning / scratch
claude_tasks/

# Claude Code — personal/local only (keep .claude/settings.json shareable)
.claude/settings.local.json
.claude/cache/
