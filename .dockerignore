# Docker config files (no need inside the image)
Dockerfile
docker-compose.yml
.dockerignore

# Git / IDE / Devcontainer
.git
.gitignore
.devcontainer
.vscode
.idea

# Python caches
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv
venv/
.venv/

# Env files — loaded at runtime via env_file, never baked in
.env
.env.example

# Local outputs (container generates its own)
output/
logs/
*.log

# Claude planning / scratch
claude_tasks/

# Claude Code — never needed inside the image
.claude/
